{
  "name": "tourmanager-submit-form",
  "type": "module",
  "version": "1.0.0",
  "scripts": {
    "dev:api": "node dev-api.js",
    "dev:frontend": "cd frontend && npm run dev",
    "build": "cd frontend && npm run build"
  },
  "dependencies": {
    "pg": "^8.16.3"
  },
  "devDependencies": {
    "dotenv": "^16.3.1",
    "express": "^4.18.2"
  }
}
